'use strict';

var assert = require('chai').assert,
	sinon = require('sinon'),
	disputeViewModel = require('../../../models/disputeViewModel');

describe('disputeViewModel', function () {
	var data = {

	}, models = {
		sellerModel: {}
	}, formatter = {};

	beforeEach(function (next) {
		next();
	});
});